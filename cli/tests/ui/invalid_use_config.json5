{
  manifest_version: "0.4.0",
  use: {
    overlay: {
      manifest: "./invalid_use_config_overlay.json5",
      config: {
        token: "${config.missing}",
      },
    },
  },
  overlays: ["#overlay.apply"],
}
