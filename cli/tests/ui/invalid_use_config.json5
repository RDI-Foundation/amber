{
  manifest_version: "0.4.0",
  use: {
    policy: {
      manifest: "./invalid_use_config_policy.json5",
      config: {
        token: "${config.missing}",
      },
    },
  },
  policies: ["#policy.apply"],
}
